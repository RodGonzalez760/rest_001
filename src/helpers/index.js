// 40 exportamos la capa
module.exports = {
    JwtHelper: require('./jwt.helper'),
    CACHE_TIME: require('./cache-time.helper')
};


// 41 habiendo terminado vamos a crear nuestro servicio de autenticación en \services\

// 56 vamos a hacer un cache a nuestros usuarios a las rutas en user.routes.js
